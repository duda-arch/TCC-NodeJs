<%- include('../partials/header.ejs')%>


<% if(obj.length <= 0 ) { %>

	<%- include('../partials/carrinhoSem.ejs')%>

<% }else{  %>

<div class="container">
		<div class="col-12">
		<div class="row">
	

		</div>
				<div style="margin-top: 10px;" class="row">
					<div class="product">
				<% obj.forEach((el,index)=>{ %>

				
					<div  style="margin-left: 3em; "class="col cont-product">
						<img style="margin-top: 10px;" class="img-responsive" width="200" height="200" src="../products/<%=el.img%>">
						
						<a href="/product/<%=el.slug%>">
							<button style="margin-left: 5em;" type="button" class="btn btn-primary btn-sm btn-block">
								<span class="glyphicon glyphicon-share-alt"></span> Continue comprando
							</button>
						</a>
					<div class="col-6">
						<h3  style="margin-top: 10px; " ><%=el.name%></h3>
						<p style="margin-top: 10px; " class="card-text  more">	<%=el.description%></p>
						<button type="button" class="btn btn-info btn-sm btn-block">
							<span class="glyphicon glyphicon-share-alt"></span> <%=el.slug%>
						</button>
					</div>
					<form action="/delete" method="post">
					<div style="margin-left: 20em; display: left;"  id="price">
						<p>R$ <%=el.price%>  <button   type="submit" class="btn btn-outline-danger btn-md  " role="button" aria-disabled="true"><img src="../img/lixeira-de-reciclagem.png" width="20px" alt="" srcset=""></button></p>
						<input type="hidden" name="id" value="<%=index%>">
					</div>
				</form>
					<hr>
					<hr>
</div>
				

				<%	})  %>
				</div>
				<div style="padding-top: 4em;" class="col-4 bg-secondary">
					<form  onsubmit="return check_form2()" >
					<label for="validationCustom01"  class="form-label card-header ">Nome da Rua</label>
					<input  id="validationCustom01" type="text" class="form-control log2" id="inputAddress2" placeholder="Endereço Completo">
					<label class="form-label card-header">Nome da Casa</label>
					<input type="text" class="form-control log2" id="inputAddress2" placeholder="Casa Number">
					<label class="form-label card-header">Cep</label>
					<input type="text" class="form-control log2" id="inputAddress2" placeholder="Endereço Postal">
					<button  style="margin-left: 15em; margin-top: 10px; border-radius: 10px; padding: 6px" type="submit" class="btn-dark">Calcular frete</button>

					<p id="total">R$ <%=total%></p>
					<button  type="button" class="btn btn-primary btn-sm btn-block">
						<span class="glyphicon glyphicon-share-alt"></span> Confirmar compra
					</button>
				</form>
				</div>
				

			</div>	
		</div>
</div>

<style>
	.card-header{
		color:white;
		font-family: 'Oswald', sans-serif;
		font-size: 1.5em;


	}
	.valor{
		height: 35px;
	}
	.cont-product{
		
		align-items: center;
	}
	.product{
		width: 55em;
		height: 600px;
		overflow: scroll;
		background-color: #DCDCDC;
		padding: 20;
	}

	#total{
		font-family: 'Oswald', sans-serif;
		font-size: 2em;
		margin-top: 3em;
	
	}
	#price{
		font-size: 1.5em;
	}
	#col-price{
		font-family: 'Oswald', sans-serif;

	}
</style>


<script src="https://cdnjs.cloudflare.com/ajax/libs/socket.io/4.1.3/socket.io.js"></script>
    <script>
        var socket = io('http://localhost:7856')
	
		// socket.emit('msg',{npm:price})
	
	
</script>

<script src="https://code.jquery.com/jquery-3.5.0.js"></script>
<script>
 
 function check_form2(){

var inputs = document.getElementsByClassName('log2');

var len = inputs.length;
var valid = true;
for(var i=0; i < len; i++){
 if (!inputs[i].value){ valid = false; }
        if (!valid){
        alert('Por favor preencha todos os campos.');
        return false;
        } 

    }
}
   

 $(document).ready(function() {
 
 var showChar = 150;  
 var ellipsestext = "...";
 var moretext = "Ler Mais >";
 var lesstext = "Esconder";
 

 $('.more').each(function() {
     var content = $(this).html();

     if(content.length > showChar) {

         var c = content.substr(0, showChar);
         var h = content.substr(showChar, content.length - showChar);

         var html = c + '<span class="moreellipses">' + ellipsestext+ '&nbsp;</span><span class="morecontent"><span>' + h + '</span>&nbsp;&nbsp;<a href="" class="btn btn-info morelink">' + moretext + '</a></span>';

         $(this).html(html);
     }

 });

 $(".morelink").click(function(){
     if($(this).hasClass("less")) {
         $(this).removeClass("less");
         $(this).html(moretext);
     } else {
         $(this).addClass("less");
         $(this).html(lesstext);
     }
     $(this).parent().prev().toggle();
     $(this).prev().toggle();
     return false;
 });
});

    
</script>
<style>

.morecontent span {
    display: none;
}
.morelink {
    display: block;
}
    .container-product{
        width: 100%;
        height:  400px;
        background-color: aqua;
        padding: 10px;
    }
    .container-element{
        margin-top: 10px;
        width: 100%;
        padding: 10px;
        height:  300px;
        background-color: wheat;
    }
</style>
<% } %>
<%- include('../partials/footer.ejs')%>
